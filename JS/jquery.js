/* EXO 1
 $("#jsstyle").on('click',function(){
        $("p").css("color", "red");
    }); */


/* EXO 2
var id = $("a").attr("id");
var href = $("a").attr("href");
var hreflang = $("a").attr("hreflang");
var rel = $("a").attr("rel");
var target = $("a").attr("target");
var type = $("a").attr("type");
   
    $("#values-btn").on('click',function(){
        alert('id = '+ id + ' ,href = ' + href + ' ,hreflang = ' + hreflang + ' ,rel = ' + rel + ' ,target = ' + target + ' ,type = ' + type);
    }); */

/* EXO 3
$('#myTable tr').each(function(indiceLigne, indiceColone, newText) {

    var identifiant = $(this).find(indiceLigne).eq(indiceColone).html(); //L'index 0 permet de récupérer le contenu de la première cellule de la ligne
    console.log(identifiant)
    });
*/

$("#input").on('click',function(){
        
    }); 
    
/* EXO 4 */

/* $( "select" )
  .keyup(function() {
    var value = $( this ).val();
    $("#remove-btn").on('click',function(){
    $( "p" ).text( value );
    })
  })
  .keyup() */
